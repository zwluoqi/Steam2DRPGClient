//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using Xor;

public partial class DictGuideUnit
{

    // data model
    // [System.Serializable]
    public class Model
    {
     /// <summary>
        /// 引导大步骤主键ID（主键必须是连续的从0开始）
        /// </summary>
        public XorInt guide_unit_id_xor;
     /// <summary>
        /// 引导大步骤主键ID（主键必须是连续的从0开始）
        /// </summary>
        public int guide_unit_id{
            get{
                return guide_unit_id_xor.val;
            }
        }
     /// <summary>
        /// 引导小步骤
        /// </summary>
        public List<int> guide_steps;
     /// <summary>
        /// 关键步骤
        /// </summary>
        public List<int> key_steps;
     /// <summary>
        /// 等效大步骤
        /// </summary>
        public List<int> equip_unit_ids;
     /// <summary>
        /// 能否跳过
        /// </summary>
        public XorInt canJump_xor;
     /// <summary>
        /// 能否跳过
        /// </summary>
        public int canJump{
            get{
                return canJump_xor.val;
            }
        }
     /// <summary>
        /// 引导功能描述
        /// </summary>
        public string desc;

    public virtual Model Clone()
    {
        Model mm = this.MemberwiseClone() as Model;
        return mm;
    }
}

    //
    private Dictionary<int, Model> m_dict;

    // Get The Dictionary
    public Dictionary<int, Model> Dict
    {
        get
        {
            return m_dict;
        }
    }

    private List<Model> m_list;
	public List<Model> getList()
	{
		return m_list;
	}

    // Get The Model By Key
    public Model GetModel(int guide_unit_id)
    {
        if (m_dict == null)
        {
            Debug.LogError("DictGuideUnit m_dict Is Null");
            return null;
        }
        else
        {
            if (m_dict.ContainsKey(guide_unit_id))
            {
                return m_dict[guide_unit_id];
            }
            else
            {
                Debug.LogError ("error id:"+guide_unit_id);
                return null;
            }
        }
    }

    private DictString_LoadFinishedCallBack m_loadFinishedCallBack;
    // load the json file
    public void Load(string path,DictString_LoadFinishedCallBack callBack)
    {
        m_loadFinishedCallBack = callBack;

        //
        string filePath = path;
        if(!filePath.EndsWith("/"))
        {
            filePath += "/";
        }

        filePath += GetFileName();
        DictFileReader fileReader = new DictFileReader(filePath, DoParse);
    }

    // get file name
    public string GetFileName()
    {
        return "dict_guide_unit.txt";
    }

    // parse the json data
    private void DoParse(DictFileReader fileReader)
    {
        m_dict = new Dictionary<int,Model>();
        m_list = new List<Model>();
        //
        do
        {
            string[] str = fileReader.ReadRow();
            if (str == null || str.Length == 0)
            {
                break;
            }

            //
            Model model = new Model();
            model.guide_unit_id_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["guide_unit_id"]]),fileReader.randomUtil);
            model.guide_steps = DictTypeConvert.ParseArrayInt(str[fileReader.typeName2Index["guide_steps"]]);
            model.key_steps = DictTypeConvert.ParseArrayInt(str[fileReader.typeName2Index["key_steps"]]);
            model.equip_unit_ids = DictTypeConvert.ParseArrayInt(str[fileReader.typeName2Index["equip_unit_ids"]]);
            model.canJump_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["canJump"]]),fileReader.randomUtil);
            model.desc = DictTypeConvert.ParseString(str[fileReader.typeName2Index["desc"]]);

            if (m_dict.ContainsKey(model.guide_unit_id) == false)
            {
                m_dict.Add(model.guide_unit_id, model);
                m_list.Add(model);
            }
            else
            {
                Debug.LogError("DictGuideUnit Parse:Same Key = " + model.guide_unit_id);
            }

        } while (true);

        if(m_loadFinishedCallBack != null)
        {
            m_loadFinishedCallBack(GetFileName());
        }
    }

}
