//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using Xor;

public partial class DictDongfuBuildProduce
{

    // data model
    // [System.Serializable]
    public class Model
    {
     /// <summary>
        /// 英雄id
        /// </summary>
        public XorInt id_xor;
     /// <summary>
        /// 英雄id
        /// </summary>
        public int id{
            get{
                return id_xor.val;
            }
        }
     /// <summary>
        /// 名字
        /// </summary>
        public string name;
     /// <summary>
        /// 建筑
        /// </summary>
        public XorInt build_id_xor;
     /// <summary>
        /// 建筑
        /// </summary>
        public int build_id{
            get{
                return build_id_xor.val;
            }
        }
     /// <summary>
        /// 解锁等级
        /// </summary>
        public XorInt lev_xor;
     /// <summary>
        /// 解锁等级
        /// </summary>
        public int lev{
            get{
                return lev_xor.val;
            }
        }
     /// <summary>
        /// 解锁物品
        /// </summary>
        public string good_id;
     /// <summary>
        /// 生产效率（分/个）
        /// </summary>
        public XorInt produce_cost_xor;
     /// <summary>
        /// 生产效率（分/个）
        /// </summary>
        public int produce_cost{
            get{
                return produce_cost_xor.val;
            }
        }

    public virtual Model Clone()
    {
        Model mm = this.MemberwiseClone() as Model;
        return mm;
    }
}

    //
    private Dictionary<int, Model> m_dict;

    // Get The Dictionary
    public Dictionary<int, Model> Dict
    {
        get
        {
            return m_dict;
        }
    }

    private List<Model> m_list;
	public List<Model> getList()
	{
		return m_list;
	}

    // Get The Model By Key
    public Model GetModel(int id)
    {
        if (m_dict == null)
        {
            Debug.LogError("DictDongfuBuildProduce m_dict Is Null");
            return null;
        }
        else
        {
            if (m_dict.ContainsKey(id))
            {
                return m_dict[id];
            }
            else
            {
                Debug.LogError ("error id:"+id);
                return null;
            }
        }
    }

    private DictString_LoadFinishedCallBack m_loadFinishedCallBack;
    // load the json file
    public void Load(string path,DictString_LoadFinishedCallBack callBack)
    {
        m_loadFinishedCallBack = callBack;

        //
        string filePath = path;
        if(!filePath.EndsWith("/"))
        {
            filePath += "/";
        }

        filePath += GetFileName();
        DictFileReader fileReader = new DictFileReader(filePath, DoParse);
    }

    // get file name
    public string GetFileName()
    {
        return "dict_dongfu_build_produce.txt";
    }

    // parse the json data
    private void DoParse(DictFileReader fileReader)
    {
        m_dict = new Dictionary<int,Model>();
        m_list = new List<Model>();
        //
        do
        {
            string[] str = fileReader.ReadRow();
            if (str == null || str.Length == 0)
            {
                break;
            }

            //
            Model model = new Model();
            model.id_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["id"]]),fileReader.randomUtil);
            model.name = DictTypeConvert.ParseString(str[fileReader.typeName2Index["name"]]);
            model.build_id_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["build_id"]]),fileReader.randomUtil);
            model.lev_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["lev"]]),fileReader.randomUtil);
            model.good_id = DictTypeConvert.ParseString(str[fileReader.typeName2Index["good_id"]]);
            model.produce_cost_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["produce_cost"]]),fileReader.randomUtil);

            if (m_dict.ContainsKey(model.id) == false)
            {
                m_dict.Add(model.id, model);
                m_list.Add(model);
            }
            else
            {
                Debug.LogError("DictDongfuBuildProduce Parse:Same Key = " + model.id);
            }

        } while (true);

        if(m_loadFinishedCallBack != null)
        {
            m_loadFinishedCallBack(GetFileName());
        }
    }

}
