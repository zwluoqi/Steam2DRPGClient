//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using Xor;

public partial class DictRechargeContinueLoginGift
{

    // data model
    // [System.Serializable]
    public class Model
    {
     /// <summary>
        /// vip等级
        /// </summary>
        public XorInt id_xor;
     /// <summary>
        /// vip等级
        /// </summary>
        public int id{
            get{
                return id_xor.val;
            }
        }
     /// <summary>
        /// 标题
        /// </summary>
        public string title;
     /// <summary>
        /// 内容
        /// </summary>
        public string content;
     /// <summary>
        /// 登陆数据
        /// </summary>
        public XorInt login_data_xor;
     /// <summary>
        /// 登陆数据
        /// </summary>
        public int login_data{
            get{
                return login_data_xor.val;
            }
        }
     /// <summary>
        /// 起始日期
        /// </summary>
        public XorInt login_count_xor;
     /// <summary>
        /// 起始日期
        /// </summary>
        public int login_count{
            get{
                return login_count_xor.val;
            }
        }
     /// <summary>
        /// 赠送礼包
        /// </summary>
        public List<string> send_rewards;

    public virtual Model Clone()
    {
        Model mm = this.MemberwiseClone() as Model;
        return mm;
    }
}

    //
    private Dictionary<int, Model> m_dict;

    // Get The Dictionary
    public Dictionary<int, Model> Dict
    {
        get
        {
            return m_dict;
        }
    }

    private List<Model> m_list;
	public List<Model> getList()
	{
		return m_list;
	}

    // Get The Model By Key
    public Model GetModel(int id)
    {
        if (m_dict == null)
        {
            Debug.LogError("DictRechargeContinueLoginGift m_dict Is Null");
            return null;
        }
        else
        {
            if (m_dict.ContainsKey(id))
            {
                return m_dict[id];
            }
            else
            {
                Debug.LogError ("error id:"+id);
                return null;
            }
        }
    }

    private DictString_LoadFinishedCallBack m_loadFinishedCallBack;
    // load the json file
    public void Load(string path,DictString_LoadFinishedCallBack callBack)
    {
        m_loadFinishedCallBack = callBack;

        //
        string filePath = path;
        if(!filePath.EndsWith("/"))
        {
            filePath += "/";
        }

        filePath += GetFileName();
        DictFileReader fileReader = new DictFileReader(filePath, DoParse);
    }

    // get file name
    public string GetFileName()
    {
        return "dict_recharge_continue_login_gift.txt";
    }

    // parse the json data
    private void DoParse(DictFileReader fileReader)
    {
        m_dict = new Dictionary<int,Model>();
        m_list = new List<Model>();
        //
        do
        {
            string[] str = fileReader.ReadRow();
            if (str == null || str.Length == 0)
            {
                break;
            }

            //
            Model model = new Model();
            model.id_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["id"]]),fileReader.randomUtil);
            model.title = DictTypeConvert.ParseString(str[fileReader.typeName2Index["title"]]);
            model.content = DictTypeConvert.ParseString(str[fileReader.typeName2Index["content"]]);
            model.login_data_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["login_data"]]),fileReader.randomUtil);
            model.login_count_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["login_count"]]),fileReader.randomUtil);
            model.send_rewards = DictTypeConvert.ParseArrayString(str[fileReader.typeName2Index["send_rewards"]]);

            if (m_dict.ContainsKey(model.id) == false)
            {
                m_dict.Add(model.id, model);
                m_list.Add(model);
            }
            else
            {
                Debug.LogError("DictRechargeContinueLoginGift Parse:Same Key = " + model.id);
            }

        } while (true);

        if(m_loadFinishedCallBack != null)
        {
            m_loadFinishedCallBack(GetFileName());
        }
    }

}
