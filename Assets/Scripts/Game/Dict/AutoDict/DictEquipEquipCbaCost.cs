//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using Xor;

public partial class DictEquipEquipCbaCost
{

    // data model
    // [System.Serializable]
    public class Model
    {
     /// <summary>
        /// 主键
        /// </summary>
        public XorInt id_xor;
     /// <summary>
        /// 主键
        /// </summary>
        public int id{
            get{
                return id_xor.val;
            }
        }
     /// <summary>
        /// 目标ID
        /// </summary>
        public string target_id;
     /// <summary>
        /// 装备ID
        /// </summary>
        public string equip_id;
     /// <summary>
        /// 物品ID
        /// </summary>
        public string good_id;
     /// <summary>
        /// 消耗class
        /// </summary>
        public string cost_classId;
     /// <summary>
        /// 消耗prop
        /// </summary>
        public string cost_propId;
     /// <summary>
        /// 消耗obj
        /// </summary>
        public string cost_objId;
     /// <summary>
        /// 消耗数量
        /// </summary>
        public XorInt cost_num_xor;
     /// <summary>
        /// 消耗数量
        /// </summary>
        public int cost_num{
            get{
                return cost_num_xor.val;
            }
        }

    public virtual Model Clone()
    {
        Model mm = this.MemberwiseClone() as Model;
        return mm;
    }
}

    //
    private Dictionary<int, Model> m_dict;

    // Get The Dictionary
    public Dictionary<int, Model> Dict
    {
        get
        {
            return m_dict;
        }
    }

    private List<Model> m_list;
	public List<Model> getList()
	{
		return m_list;
	}

    // Get The Model By Key
    public Model GetModel(int id)
    {
        if (m_dict == null)
        {
            Debug.LogError("DictEquipEquipCbaCost m_dict Is Null");
            return null;
        }
        else
        {
            if (m_dict.ContainsKey(id))
            {
                return m_dict[id];
            }
            else
            {
                Debug.LogError ("error id:"+id);
                return null;
            }
        }
    }

    private DictString_LoadFinishedCallBack m_loadFinishedCallBack;
    // load the json file
    public void Load(string path,DictString_LoadFinishedCallBack callBack)
    {
        m_loadFinishedCallBack = callBack;

        //
        string filePath = path;
        if(!filePath.EndsWith("/"))
        {
            filePath += "/";
        }

        filePath += GetFileName();
        DictFileReader fileReader = new DictFileReader(filePath, DoParse);
    }

    // get file name
    public string GetFileName()
    {
        return "dict_equip_equip_cba_cost.txt";
    }

    // parse the json data
    private void DoParse(DictFileReader fileReader)
    {
        m_dict = new Dictionary<int,Model>();
        m_list = new List<Model>();
        //
        do
        {
            string[] str = fileReader.ReadRow();
            if (str == null || str.Length == 0)
            {
                break;
            }

            //
            Model model = new Model();
            model.id_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["id"]]),fileReader.randomUtil);
            model.target_id = DictTypeConvert.ParseString(str[fileReader.typeName2Index["target_id"]]);
            model.equip_id = DictTypeConvert.ParseString(str[fileReader.typeName2Index["equip_id"]]);
            model.good_id = DictTypeConvert.ParseString(str[fileReader.typeName2Index["good_id"]]);
            model.cost_classId = DictTypeConvert.ParseString(str[fileReader.typeName2Index["cost_classId"]]);
            model.cost_propId = DictTypeConvert.ParseString(str[fileReader.typeName2Index["cost_propId"]]);
            model.cost_objId = DictTypeConvert.ParseString(str[fileReader.typeName2Index["cost_objId"]]);
            model.cost_num_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["cost_num"]]),fileReader.randomUtil);

            if (m_dict.ContainsKey(model.id) == false)
            {
                m_dict.Add(model.id, model);
                m_list.Add(model);
            }
            else
            {
                Debug.LogError("DictEquipEquipCbaCost Parse:Same Key = " + model.id);
            }

        } while (true);

        if(m_loadFinishedCallBack != null)
        {
            m_loadFinishedCallBack(GetFileName());
        }
    }

}
