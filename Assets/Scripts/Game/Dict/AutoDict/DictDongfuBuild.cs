//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by a tool.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using Xor;

public partial class DictDongfuBuild
{

    // data model
    // [System.Serializable]
    public class Model
    {
     /// <summary>
        /// 英雄id
        /// </summary>
        public XorInt id_xor;
     /// <summary>
        /// 英雄id
        /// </summary>
        public int id{
            get{
                return id_xor.val;
            }
        }
     /// <summary>
        /// 名字
        /// </summary>
        public string name;
     /// <summary>
        /// 建筑效果
        /// </summary>
        public string build_desc;
     /// <summary>
        /// 英雄效果
        /// </summary>
        public string hero_desc;
     /// <summary>
        /// 建造消耗
        /// </summary>
        public List<string> build_cost;
     /// <summary>
        /// 前置建筑
        /// </summary>
        public List<int> before_build_id;
     /// <summary>
        /// 前置建筑等级
        /// </summary>
        public List<int> before_build_lev;

    public virtual Model Clone()
    {
        Model mm = this.MemberwiseClone() as Model;
        return mm;
    }
}

    //
    private Dictionary<int, Model> m_dict;

    // Get The Dictionary
    public Dictionary<int, Model> Dict
    {
        get
        {
            return m_dict;
        }
    }

    private List<Model> m_list;
	public List<Model> getList()
	{
		return m_list;
	}

    // Get The Model By Key
    public Model GetModel(int id)
    {
        if (m_dict == null)
        {
            Debug.LogError("DictDongfuBuild m_dict Is Null");
            return null;
        }
        else
        {
            if (m_dict.ContainsKey(id))
            {
                return m_dict[id];
            }
            else
            {
                Debug.LogError ("error id:"+id);
                return null;
            }
        }
    }

    private DictString_LoadFinishedCallBack m_loadFinishedCallBack;
    // load the json file
    public void Load(string path,DictString_LoadFinishedCallBack callBack)
    {
        m_loadFinishedCallBack = callBack;

        //
        string filePath = path;
        if(!filePath.EndsWith("/"))
        {
            filePath += "/";
        }

        filePath += GetFileName();
        DictFileReader fileReader = new DictFileReader(filePath, DoParse);
    }

    // get file name
    public string GetFileName()
    {
        return "dict_dongfu_build.txt";
    }

    // parse the json data
    private void DoParse(DictFileReader fileReader)
    {
        m_dict = new Dictionary<int,Model>();
        m_list = new List<Model>();
        //
        do
        {
            string[] str = fileReader.ReadRow();
            if (str == null || str.Length == 0)
            {
                break;
            }

            //
            Model model = new Model();
            model.id_xor = new XorInt( DictTypeConvert.ParseInt(str[fileReader.typeName2Index["id"]]),fileReader.randomUtil);
            model.name = DictTypeConvert.ParseString(str[fileReader.typeName2Index["name"]]);
            model.build_desc = DictTypeConvert.ParseString(str[fileReader.typeName2Index["build_desc"]]);
            model.hero_desc = DictTypeConvert.ParseString(str[fileReader.typeName2Index["hero_desc"]]);
            model.build_cost = DictTypeConvert.ParseArrayString(str[fileReader.typeName2Index["build_cost"]]);
            model.before_build_id = DictTypeConvert.ParseArrayInt(str[fileReader.typeName2Index["before_build_id"]]);
            model.before_build_lev = DictTypeConvert.ParseArrayInt(str[fileReader.typeName2Index["before_build_lev"]]);

            if (m_dict.ContainsKey(model.id) == false)
            {
                m_dict.Add(model.id, model);
                m_list.Add(model);
            }
            else
            {
                Debug.LogError("DictDongfuBuild Parse:Same Key = " + model.id);
            }

        } while (true);

        if(m_loadFinishedCallBack != null)
        {
            m_loadFinishedCallBack(GetFileName());
        }
    }

}
